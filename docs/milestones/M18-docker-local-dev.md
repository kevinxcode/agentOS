
==================================================
MILESTONE 18 — DOCKER AND LOCAL DEVELOPMENT
==================================================

Verify local development experience.

Audit:

Dockerfile
docker-compose.yml
database service
environment variables
volumes
ports
health checks

Aim for an experience similar to:

git clone
copy .env.example
docker compose up -d
install
migrate
run dev

Use the repository's real commands.

